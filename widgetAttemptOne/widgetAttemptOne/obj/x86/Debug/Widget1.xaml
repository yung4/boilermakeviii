<Page
    x:Class="widgetAttemptOne.Widget1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:widgetAttemptOne"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" />

        

        <StackPanel
            Margin="0,0,0,10"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Orientation="Vertical"
            DataContext="{RelativeSource Self}">
            <Image x:ConnectionId='2' x:Name="myImage" Source="Assets/breach.png" Height="200" Width="200" VerticalAlignment="Top" Margin="10"/>
            <TextBlock x:ConnectionId='3' x:Name="myLabel" Margin="0,0,0,10">Click the button to select a random agent!</TextBlock>
            <Button x:ConnectionId='4' x:Name="myButton"                        HorizontalAlignment="Center">Randomize</Button>
        </StackPanel>
    </Grid>
</Page>

